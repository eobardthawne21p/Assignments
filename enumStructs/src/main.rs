fn main() {
    /* #[derive(Debug)]
    enum PaymentMethod {
        Cash,
        CreditCard,
        CryptoCurrency
    }

    let cash: PaymentMethod = PaymentMethod::Cash;
    let cc: PaymentMethod = PaymentMethod::CreditCard;
    let crc: PaymentMethod = PaymentMethod::CryptoCurrency;

    println!("{:?}",cash);
    println!("{:?}",cc);
    println!("{:?}",crc); */

   /* #[derive(Debug)]
    #[allow(dead_code)]
    enum Genre {
        Fiction,
        NonFiction,
        ScienceFiction,
        Fantasy,
    }
    
    struct Book {
        title: String,
        genre: Genre,
        review_score: u8,
    }
    
    
    let my_book = Book {
        title: String::from("Rust Programming"),
        genre: Genre::NonFiction,
        review_score: 5,
    };

    let my_other_book = Book {
        title: String::from("A Game of Thrones"),
        genre: Genre::Fantasy,
        review_score: 5,
    };

    println!("{} is a {:?} book with a score of {}", my_book.title, my_book.genre, my_book.review_score);
    println!("{} is a {:?} book with a score of {}", my_other_book.title, my_other_book.genre, my_other_book.review_score); */

    
}